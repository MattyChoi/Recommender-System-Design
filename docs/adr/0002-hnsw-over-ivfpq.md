# ADR 0002 — HNSW over IVF-PQ

**Status:** proposed · **Date:** `TODO`

## Context

`TODO` — recall/latency requirements at the retrieval stage, index build time, and update
cadence (hourly refresh per docs/design.md).

## Decision

`TODO`

## Consequences

`TODO` — memory footprint vs. IVF-PQ, incremental-update story, and the fact that at the
current catalog size the index is demonstrative (see ADR 0001, consequence 2).

## Alternatives considered

`TODO` — IVF-PQ, ScaNN, brute force.
